services:
  moggify:
    build:
      context: .
      dockerfile: Dockerfile
    image: moggify
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
